CREATE TABLE IF NOT EXISTS `codenome_db`.`cell_coords` (
  `center` INT NOT NULL,
  `v1` INT NOT NULL,
  `v2` INT NOT NULL,
  `v3` INT NOT NULL,
  `v4` INT NOT NULL,
  `v5` INT NOT NULL,
  `v6` INT,
  PRIMARY KEY (`center`));
