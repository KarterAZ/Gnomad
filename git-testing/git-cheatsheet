#------------------------------- Legend ----------------------------------#
# <some-text> - indicates a required parameter
# The main branch is called main
# DO NOT DELETE REMOTE BRANCHES
#-------------------------------------------------------------------------#


#-------------------------- Common Commands ------------------------------#
# create a local branch:
git branch <local-branch-name>

# create a local branch and switch to it:
git checkout -b <local-branch-name>

# delete a local branch
git branch -d <local-branch>

# switch branches
git checkout <local-branch>

# list local branches
git branch -l

# list remote branches
git branch -r

# merge two branches
git merge <source-branch> <destination-branch>

# attach a local branch to a remote upstream branch
git branch --set-upstream-to=origin/<remote-branch> <local-branch>

# pull updates from remote
git pull

# push updates back to remote (always pull first)
git push